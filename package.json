{
  "name": "payload-monorepo-template",
  "packageManager": "yarn@3.2.0",
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "cross-env": "^7.0.3"
  },
  "scripts": {
    "ts-payload:dev": "yarn workspace @payload-monorepo/ts-payload dev",
    "ts-payload:generate:types": "yarn workspace @payload-monorepo/ts-payload generate:types"
  }
}